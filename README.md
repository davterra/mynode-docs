# nyNode Documentation

## Build the Documentation Locally

In order to build the website locally, you'll need Node.js >= 10.16.

1. Install dependencies
2. Serve locally (by default on port 8080)

```sh
yarn install
vuepress dev docs
```